<%= form_with(model: character) do |form| %>
  <% if character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(character.errors.count, "error") %> prohibited this character from being saved:</h2>

      <ul>
        <% character.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :info %>
    <%= form.text_area :info %>
  </div>

  <div class="field">
    <%= form.label :life_values %>
    <%= form.number_field :life_values %>
  </div>

  <div class="field">
    <%= form.label :races_id %>
    <%= form.text_field :races_id %>
  </div>

  <div class="field">
    <%= form.label :levels_id %>
    <%= form.text_field :levels_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
